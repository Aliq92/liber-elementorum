{
  "name": "arcane-interface",
  "version": "1.0.0",
  "description": "An interactive elemental archive — a magical holographic spell console with a historically-sourced spellbook.",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "python serve.py",
    "verify": "python verify.py",
    "cap:sync": "npm run build && npx cap sync",
    "android:open": "npx cap open android",
    "android:build": "npm run cap:sync && cd android && ./gradlew.bat assembleDebug"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
